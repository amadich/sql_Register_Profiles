import React from "react";
import { Component } from "react";

export default class Footer extends Component {
    render() {
        return(
            <footer>

                <label>
                    All Copy Right In GitHub (amadich)
                </label>

            </footer>
        )
    }
}